#include <bits/stdc++.h>
using namespace std;
int maxProduct(vector <int> &a)
{
    int n = a.size();
    sort(a.begin() , a.end());
    return ((a[n - 1] - 1) * (a[n - 2] - 1));
}
int main()
{
    vector <int> a = {-1, 2, 4};
    cout << maxProduct(a) << '\n';
}
